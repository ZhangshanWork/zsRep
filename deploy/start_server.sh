#!/usr/bin/env bash
if [ $# -eq 2 ]; then
    echo "The num of arguments is not correct"
    echo "usage: deploy.sh [env] [projectVersion]
    echo "env----the deployment environment"
    echo "projectVersion----the version of the project"
fi



